[project]
name = "kuill"
version = "0.1.3"
description = "A simple open source tool to organize your scientific knowledge."
requires-python = ">=3.10"
license = "GPL-3.0-only"
dependencies = [
    "textual>=7.0.0",
]

[project.scripts]
kuill = "kuill:main"
